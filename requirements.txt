# AI Literacy Bibliometric Analysis
# Python 3.13 (tested); Python 3.11+ recommended for hdbscan binary wheels
# Install: pip install -r requirements.txt

# Core data
pandas>=2.3
numpy>=2.3
scipy>=1.16
openpyxl>=3.1          # Excel export (.xlsx)
pyarrow>=20.0          # fast Parquet/CSV I/O

# Visualization
matplotlib>=3.10
seaborn>=0.13
plotly>=6.3            # interactive choropleth (RQ1 pipeline)
wordcloud>=1.9         # keyword cloud (RQ1 pipeline)

# Network analysis
networkx>=3.5
python-louvain>=0.16   # community detection (Louvain)

# Geographic analysis
geopandas>=1.1
pycountry>=26.2
pyproj>=3.7
shapely>=2.0

# NLP / topic modeling
sentence-transformers>=5.5
umap-learn>=0.5.12
hdbscan>=0.8.44
bertopic>=0.17.4
scikit-learn>=1.7
transformers>=4.57
tokenizers>=0.22
safetensors>=0.8
torch>=2.12            # CPU-only is fine; set PYTORCH_ENABLE_MPS_FALLBACK=1 on Apple Silicon
